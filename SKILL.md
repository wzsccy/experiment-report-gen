---
name: experiment-report
description: "从 config.json 生成标准格式的 .docx 实验报告，支持实验报告模式和通用模式。当用户提到实验报告、docx报告、生成报告、从results.json生成报告时使用此技能。"
---

# 实验报告生成器

从 JSON 配置文件生成标准格式的 `.docx` 报告。

## 前置条件

```bash
npm install -g experiment-report-gen
```

CLI 命令：
- `experiment-report generate <config.json>` — 从配置生成 .docx
- `experiment-report analyze <results.json> [--type image|text|auto]` — 自动生成分析文本

## 工作流程

```
config.json → experiment-report generate → .docx 报告
```

## Step 1: 确认报告模式

用 AskUserQuestion 询问报告模式：

| 选项 | 说明 | config 标志 |
|------|------|------------|
| 实验报告模式（默认） | 固定 4.1-4.5 + 5.1-5.6 结构，内置封面 | 无 `mode` 或 `"mode": "experiment"` |
| 通用模式 | 自由章节，适合非实验文档 | `"mode": "generic"` |
| 自定义模板 | 用自己的 .docx 合并封面 | `"cover_template": "<绝对路径>"` |

选"自定义模板"后追问路径。

## Step 2: 自动生成分析文本（实验报告模式）

```bash
experiment-report analyze <project-dir>/output/results.json --type image|text|auto
```

## Step 3: 准备 config.json（总字数 ≥ 6000）

**禁止使用 markdown 符号**：config.json 的所有文本字段（sections、tables、images 等）中不得出现以下符号：
- `**`（加粗标记）
- `###` / `##` / `#`（标题标记）
- `- `（无序列表标记）
- `` ` ``（行内代码标记）
- `>`（引用标记）

正确写法：`4.1 环境搭建与问题分析`（不要写 `### 4.1 环境搭建与问题分析`）
正确写法：`问题一：中文字体显示问题`（不要写 `**问题一：中文字体显示问题**`）
正确写法：`第1层：Linear(100, 256)`（不要写 `- 第1层：Linear(100, 256)`）

**至少 3 个数据表格**（模型对比/超参数/数据集统计/性能对比/训练过程），数据从 `results.json` 提取。

**purpose（≥ 500 字）：**
- 一到两个完整段落，说明实验背景、主要任务、期望收获
- 不要写成论文摘要，语气自然，像学生写的实验目的

**content（≥ 2500 字）：**
- 4.1 环境搭建与问题分析（300+）：环境配置、关键报错及解决，简明扼要即可
- 4.2 数据集与预处理（400+）：数据来源/规模/类别、归一化/增强、DataLoader
- 4.3 模型结构设计（600+）：逐层描述、关键组件、参数量、为什么选择这个架构、与其他方案的取舍
- 4.4 训练策略与优化（600+）：超参数选择理由、优化器、lr 调度、正则化、梯度裁剪，解释每项设置的原因
- 4.5 训练过程分析（600+）：初期/中期/后期的损失和指标变化，结合具体数值分析趋势背后的原因

**results（≥ 2000 字）：**
- 5.1 损失函数分析（350+）：引用实际数值、下降趋势、过拟合判断
- 5.2 准确率/精度分析（350+）：引用数值、变化趋势、是否达标
- 5.3 分类/预测结果分析（400+）：预测图描述、正确/错误原因、典型样本的具体分析
- 5.4 泛化能力分析（250+）：训练 vs 测试差距、正则化效果
- 5.5 优化策略效果（250+）：BatchNorm/数据增强/lr 调度效果
- 5.6 综合评价（150+）：整体性能、是否达标

**thought_questions（≥ 1000 字）：** 逐题回答，结合数据和理论，分点论述。每题至少 400 字。要有深度，不能只写表面现象，要分析背后的原理。

**字数校验：** 写完 config.json 后，计算 purpose + references + tasks + content + results + thought_questions 的总字符数。不足 6000 时必须补充内容后再生成 docx。

## Step 4: 生成 docx

```bash
experiment-report generate <config-json-path>
```

## config.json 完整格式（实验报告模式）

```json
{
  "mode": "experiment",
  "cover_template": "../experiment-report/templates/实验报告样本.docx",
  "title": "实验标题",
  "student_name": "张三",
  "student_id": "20240000001",
  "class_name": "24数据科学与大数据技术",
  "teacher": "李四",
  "course": "课程名称",
  "date": "2026年5月3日",
  "semester": "2025-2026 学年第 2 学期",
  "output_path": "实验报告-实验X.docx",
  "sections": {
    "purpose": "500+字...",
    "references": "参考文献...",
    "tasks": "实验任务...",
    "content": "2500+字，含4.1-4.5...",
    "results": "2000+字，含5.1-5.6...",
    "thought_questions": "1000+字，每题至少400字..."
  },
  "images": [
    { "path": "output/curves.png", "caption": "图 1 ...", "explain": "...", "section": "results" }
  ],
  "tables": [
    { "caption": "表 1 ...", "section": "content", "position": "start", "explain": "...", "headers": ["列1","列2"], "rows": [["值1","值2"]] }
  ]
}
```

## 通用模式 config 格式

```json
{
  "mode": "generic",
  "title": "文档标题",
  "date": "2026年5月5日",
  "output_path": "输出.docx",
  "cover": {
    "title": "课 程 报 告", "subtitle": "副标题",
    "fields": [{ "label": "学    院", "value": "计算机学院" }],
    "date": "2026年5月5日"
  },
  "body_sections": [
    {
      "title": "一、章节标题", "key": "key", "content": "正文...",
      "subsections": [{ "title": "1.1 子章节", "content": "..." }],
      "tables": [{ "caption": "表 1 ...", "headers": ["列1"], "rows": [["值1"]] }]
    }
  ],
  "images": [{ "path": "output/curves.png", "caption": "图 1 ...", "section": "results" }]
}
```

与实验报告模式区别：`body_sections` 数组代替 `sections`，封面用 `cover` 对象，无固定章节结构。
config 示例：`templates/config-example.json`、`templates/config-generic-example.json`

## 字段说明

**cover_template（仅实验报告模式）：**
- `null` → 自动生成封面；docx 路径 → 提取模板封面合并正文；通用模式不用此字段
- 路径相对于 config.json 所在目录，用 `/` 或 `\\`

**images：** 数组，`section` 可选 `content`/`results`，`explain` 可选，图片缺失时跳过。

**tables：** 数组，含 `caption`/`headers`/`rows`，`section` 指定嵌入章节，`position` 可选 `start`/`end`(默认)，`explain` 可选。数据必须从 `results.json` 提取。

## 报告格式标准

**封面页：** 模板封面 → 提取替换；自动生成 → 学院名(居中) + "实 验 报 告"(居中) + 学期(居中) + 课程/实验/班级/姓名学号/教师(左对齐) + 日期(居中)

**正文页：** 标题(居中) + 时间(左对齐) + Table 0(5行1列，一~五节) + Table 1(思考题) + 图片嵌入单元格

## 写作规则

生成报告时，整体风格应接近"学生完成实验后整理出来的正式报告"。报告需要有一定篇幅，不能过于空泛，也不能只列要点。优先使用自然连贯的大段描述，把实验背景、操作过程、观察结果和分析整合起来写。可以保留必要的小标题、表格和少量编号，但不要把正文写成大量机械的项目符号。每个小节应尽量形成完整段落，让内容看起来有实际实验过程，而不是由 AI 拼接出来的提纲。

报告语言要正式、清楚、具体，但不要过度学术化。允许使用专业术语（PyTorch、GAN、损失函数、BatchNorm 等），但解释要适度。不要把每个概念都写成百科介绍，也不要在多个小节反复解释同一个技术点。技术内容应围绕本次实验本身展开，重点写实验中做了什么、为什么这样设置、运行后观察到了什么、结果有什么问题、如何分析这些现象。

正文要尽量减少 AI 化表达。不要频繁使用"本实验旨在深入理解""具有重要意义""显著提升""充分证明""奠定良好基础""成功实现所有预定目标""整体质量优秀""动态平衡现象表明"等模板化、拔高式语句。可以改成更自然的表达，例如"本次实验主要是练习……""从结果来看……""对比后可以发现……""生成结果基本能看出数字轮廓，但细节还有不足"。

优先采用"段落式叙述 + 必要表格 + 少量列表"的结构。用户如果没有明确要求大量分点，就不要把每一节都写成 1、2、3、4 的小点。对于实验目的、实验内容、训练过程分析、结果分析、思考题解答和总结，应尽量写成较完整的大段。不要每段都采用"首先、其次、再次、最后"的固定结构，也不要每段都以"本实验"开头。

实验目的部分应写得具体、自然，不要像论文摘要。可以说明这次实验主要完成了什么任务。不要把实验意义写得太宏大。实验目的可以是一到两个较完整的段落。

实验原理部分应简明介绍与实验直接相关的理论。对于常见概念，只需要说明它在本实验中的作用。不要写成教材式长篇理论，不要过度展开公式推导。

实验内容或实验步骤部分要突出真实操作过程。可以适当加入实验过程中容易遇到的问题，但这些问题必须和用户提供的信息一致，不能随意编造。

模型结构和超参数可以用表格呈现，但表格前后的文字要简洁自然，不要把表格内容完整重复一遍。

结果分析部分必须围绕实际结果写，不能空泛总结。应优先引用用户提供的损失值、曲线变化、生成图片效果等具体信息。分析生成效果时要客观，不要只写"效果良好"。如果实验结果一般，应如实写出不足。

学习率对比、参数对比的分析要先写现象，再写原因。原因分析可以保守一些，用"可能是因为""从本次结果看"这样的表达，避免过度绝对。

思考题解答部分要像学生根据实验现象写出的回答，而不是标准答案。应结合实验中的具体数据来说明问题。

总结部分要客观、克制。可以说明实验基本完成了哪些内容、结果达到了什么程度、还有哪些不足。不要写成"圆满完成""充分证明"。

**禁止 AI 化表达替换表：**

| 禁止写法 | 改为 |
|---------|------|
| 本实验旨在深入理解…… | 本次实验主要是练习…… |
| 具有重要意义 | 对理解相关内容有一定帮助 |
| 为后续学习奠定良好基础 | 对后续学习有一定参考 |
| 显著提升 | 有所改善 / 更稳定一些 |
| 充分证明 | 可以看出 / 从结果上看 |
| 整体质量优秀 | 整体效果尚可 / 多数结果可以辨认 |
| 完美实现 | 基本实现 |
| 动态平衡现象 | 两个损失逐渐趋于稳定 |
| 逼真的图像 | 较接近手写数字的图像 |
| 生成质量优秀 | 多数数字轮廓可以辨认 |
| 成功实现所有预定目标 | 实验内容基本完成 |
| 效果良好 | 效果尚可 / 基本可用 |

**去 AI 化自检清单（生成报告后必须逐项检查）：**

1. 检查是否存在过多模板化开头。如果多个段落都以"本实验""本文"开头，需要改写一部分。
2. 检查是否存在过度肯定的结论。如果出现"显著提升""充分证明"而没有足够数据支撑，应改成更保守的说法。
3. 检查是否重复解释同一概念。GAN 原理、BatchNorm、Dropout 等如果已在前文解释过，后文只需简单提到。
4. 检查段落是否过碎。如果某一节出现大量短小项目符号，应合并为自然段落。
5. 检查结果分析是否只写优点。实验结果部分必须包含一定的不足分析。
6. 检查内容是否像本科实验报告。最终文本应像学生完成实验后整理出的课程报告，而非论文或 AI 总结。

## Step 5: 验证

1. 确认报告 ≥ 6000 字（不足则补充内容后重新生成）
2. 确认 .docx 已生成
3. 告知用户报告路径
